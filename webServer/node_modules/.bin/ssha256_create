#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-ssha256/bin/ssha256_create" "$@"
  ret=$?
else 
  node  "$basedir/../node-ssha256/bin/ssha256_create" "$@"
  ret=$?
fi
exit $ret
